# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
rust-version = "1.85"
name = "lxmf"
version = "0.11.0"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Umbrella crate for LXMF-rs wire types and the high-level LXMF SDK."
documentation = "https://docs.rs/lxmf"
readme = "README.md"
keywords = [
    "lxmf",
    "sdk",
    "wire",
    "reticulum",
]
categories = [
    "network-programming",
    "api-bindings",
]
license = "EPL-2.0 OR GPL-2.0-or-later"
repository = "https://github.com/FreeTAKTeam/LXMF-rs"

[features]
default = [
    "sdk",
    "wire",
]
sdk = ["dep:lxmf-sdk"]
wire = ["dep:lxmf-core"]

[lib]
name = "lxmf"
path = "src/lib.rs"

[dependencies.lxmf-core]
version = "0.11.0"
optional = true
package = "lxmf-wire"

[dependencies.lxmf-sdk]
version = "0.11.0"
optional = true

[lints.clippy]
dbg_macro = "deny"
todo = "deny"
unwrap_used = "warn"

[lints.rust]
unsafe_code = "forbid"
unused_imports = "warn"
